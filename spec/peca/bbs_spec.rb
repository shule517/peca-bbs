require "spec_helper"

RSpec.describe Peca::Bbs do
  it "has a version number" do
    expect(Peca::Bbs::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
